#List the local brances
git branch
#shows the differences between the git repository and the local content
git diff
#grab the status of your edit, red will be unadded files, green will be added files
git status
#Creating a new branch
git  checkout -b <newBranchName>
#Merging branches, make sure you merge into the main banchr
git merge <oldBranch>

# Commiting Command
#add all files
git add --all
#commit the files
git commit -am "message of what you did"
#push
git push
#if first new Branch use this push
git push --set-upstream origin <branchName>


